import { FormValidateErrorMessageDirective } from './form-validate-error-message.directive';

describe('FormValidateErrorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidateErrorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
