import { DigitsLengthValidatorDirective } from './digits-length-validator.directive';

describe('CustomValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitsLengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
